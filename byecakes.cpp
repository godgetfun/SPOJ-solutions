#include<cstdio>
int main()
{
    while(1)
    {
            int e,f,s,m,E,F,S,M;
            scanf("%d%d%d%d%d%d%d%d",&e,&f,&s,&m,&E,&F,&S,&M);
            if(e==-1)
            break;
            if(e==0&&f==0&&s==0&&m==0)
            printf("0 0 0 0\n");
            else
            {
                max=1;
                if(e>E)
                a=a/
                
        

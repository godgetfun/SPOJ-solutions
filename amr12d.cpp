#include<iostream>
#include<cstdio>
#include<algorithm>
#include<cstring>
using namespace std;
int main()
{
    int t;
    cin>>t;
    cin.ignore();
    while(t--)
    {
              char str[11];
              char s[11];
              scanf("%s",str);
              strcpy(s,str);
              reverse(s,s+strlen(s));
              if(strcmp(s,str)!=0)
              printf("NO\n");
              else
              {
                  l=strlen(str);
                  for(i=0;i<l;i++)
                  {
                                  for(j=i;j<l;j++)
                                  {
                                                  strncpy(s,str+j,)
                                                  
                                  
                  
              
              
